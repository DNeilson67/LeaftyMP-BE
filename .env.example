# Email Configuration for Receipt Sending
# Add these to your .env file

# SMTP Configuration (Gmail example)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_ADDRESS=your-email@gmail.com
EMAIL_PASSWORD=your-app-password

# Alternative SMTP providers:
# For Outlook/Hotmail:
# SMTP_SERVER=smtp-mail.outlook.com
# SMTP_PORT=587

# For Yahoo:
# SMTP_SERVER=smtp.mail.yahoo.com
# SMTP_PORT=587

# For custom SMTP:
# SMTP_SERVER=your-smtp-server.com
# SMTP_PORT=587

# Note: If using Gmail, you need to:
# 1. Enable 2-factor authentication
# 2. Generate an "App Password" instead of using your regular password
# 3. Use the app password as EMAIL_PASSWORD

# Existing Xendit configuration (keep these as they are)
XENDIT_API_KEY=your-xendit-api-key
INVOICE_API_KEY=your-invoice-generator-api-key